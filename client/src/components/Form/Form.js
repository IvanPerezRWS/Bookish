
import React from 'react';
// allows for styling methods to be used. Must be added later
import useStyles from './styles';

const Form = () => {
    const classes= useStyles();

    return (
        <h1>Form</h1>
    );
}

export default Form;